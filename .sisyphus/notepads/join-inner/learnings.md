## Join Convenience Methods
- Added `inner()` method to `Join` class as a convenience for `of(fieldPath, JoinType.INNER)`.
- Updated documentation and tests accordingly.
- Encountered Lombok-related issues in incremental Maven builds, resolved by `mvn clean`.
